# ══════════════════════════════════════════════════════════════════
# 守望工具箱依赖清单（按用途分类）
# 说明：torch / aiohttp / latent_preview 等由 ComfyUI 核心环境提供，无需在此安装
# ══════════════════════════════════════════════════════════════════

# ── 一、图像处理基础库（图像 / 工具 / 采样 / 音频节点通用）──────────
numpy
    # 图像数组运算，使用节点：
    # 图像：图像拼接、图像裁剪、图像按边缩放、图像滑动对比、图像拼接对比、
    #       TTP图像划分尺寸、TTP图像分块处理、TTP图像分块拼接
    # 采样：局部重绘采样器
    # 工具：取色器、预览任何、保存至文件夹、从文件夹加载
    # 音频：提示音频

pillow
    # PIL 图像读写与处理，使用节点：
    # 图像：图像拼接、图像裁剪、图像按边缩放、图像拼接对比、
    #       TTP图像分块处理、TTP图像分块拼接
    # 采样：局部重绘采样器
    # 工具：保存至文件夹、预览任何、从文件夹加载

# ── 二、Tagger 反推器专用（守望-Tagger反推器🐢 / 批量反推器）────────
onnxruntime
    # ONNX 引擎推理（model.onnx / model_optimized.onnx，WD14/pixai 风格标签）

timm>=0.9.0
    # pixai-tagger-v0.9 pth 模型加载（内置 eva02 架构，离线构建无需联网下载权重）

matplotlib
    # 标签图例可视化与 CJK 中文字体探测
